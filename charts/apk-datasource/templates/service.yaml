apiVersion: v1
kind: Service
metadata:
  name: {{ include "apk-datasource.fullname" . }}
  labels:
    {{- include "apk-datasource.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apk-datasource.selectorLabels" . | nindent 4 }}
